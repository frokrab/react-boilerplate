import styled from 'styled-components';

const NotesListEntry = styled.li`
  border: 1px solid;
  min-height: 1.5em;
  overflow-wrap: break-word;
  padding-left: 0.5em;
`;

export default NotesListEntry;
